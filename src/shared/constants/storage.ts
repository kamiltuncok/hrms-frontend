export const STORAGE_KEYS = {
  AUTH: 'auth_data',
  USER: 'user_data',
  IS_AUTHENTICATED: 'isAuthenticated',
  TOKEN: 'token',
} as const;

export const COOKIE_TYPES = {
  AUTH: 'auth',
  USER: 'user',
} as const;
